package abc.abc158

import java.util.*

fun main(args: Array<String>) {
    val sc = Scanner(System.`in`)
    val s = sc.next()!!;
    if ("A" in s && "B" in s) {
        println("Yes")
    } else {
        println("No")
    }
}